name: weather
description: "AI-Driven Development in Flutter"

publish_to: "none"

version: 1.0.0+1

environment:
  sdk: ^3.11.3

dependencies:
  flutter:
    sdk: flutter

  build_runner: ^2.13.1
  cupertino_icons: ^1.0.9
  freezed: ^3.2.5
  freezed_annotation: ^3.1.0

dev_dependencies:
  flutter_test:
    sdk: flutter

  flutter_lints: ^6.0.0

flutter:
  uses-material-design: true
